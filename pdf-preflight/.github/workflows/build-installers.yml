name: Build Multi-Platform Installers

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Build JAR
      run: |
        mvn clean package -DskipTests
    
    - name: Build macOS Installer
      run: |
        chmod +x build-installer.sh
        ./build-installer.sh
    
    - name: Upload macOS Installer
      uses: actions/upload-artifact@v3
      with:
        name: pratt-pdf-checker-macos
        path: dist/*.dmg
    
    - name: Upload to GitHub Releases (on tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*.dmg
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Build JAR
      run: mvn clean package -DskipTests
    
    - name: Build Windows Installer
      run: .\build-installer.bat
    
    - name: Upload Windows Installer
      uses: actions/upload-artifact@v3
      with:
        name: pratt-pdf-checker-windows
        path: dist/*.exe
    
    - name: Upload to GitHub Releases (on tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-linux:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Install packaging tools
      run: |
        sudo apt-get update
        sudo apt-get install -y rpm
    
    - name: Build JAR
      run: mvn clean package -DskipTests
    
    - name: Build Linux Installer
      run: |
        chmod +x build-installer-linux.sh
        ./build-installer-linux.sh
    
    - name: Upload Linux Installers
      uses: actions/upload-artifact@v3
      with:
        name: pratt-pdf-checker-linux
        path: dist/*
    
    - name: Upload to GitHub Releases (on tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
